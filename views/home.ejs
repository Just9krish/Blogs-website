<!DOCTYPE html>
<html lang="en">
  <%-include("./partials/head.ejs") %>

  <body class="min-h-screen flex flex-col">
    <%-include("./partials/navbar.ejs") %>

    <section class="text-[#181826] flex-1">
      <div
        class="md:grid max-w-screen-xl px-4 py-8 mx-auto lg:gap-8 xl:gap-0 lg:py-16 lg:grid-cols-12"
      >
        <div class="mr-auto hero-text place-self-center lg:col-span-7">
          <h1
            class="max-w-2xl mb-4 text-4xl font-extrabold md:text-5xl xl:text-6xl"
          >
            Unlocking the
            <span class="text-[#007DFE]">Secrets</span>
            of Web Development
          </h1>
          <p class="max-w-2xl mb-6 font-light md:text-lg lg:text-xl">
            Welcome to my blog's! Here you will find tips, tricks and unique
            insights on the latest technologies and trends in the industry to
            help you improve you skills and stay ahead of the curve. Join me on
            this journey to unlock the secrets of web development and evolve
            with technology.
          </p>
        </div>
        <div class="lg:mt-0 lg:col-span-5 lg:flex">
          <img src="/img/hero.jpg" alt="A beautiful landscape" />
        </div>
      </div>
    </section>

    <section class="lg:px-32">
      <div class="px-4 py-8 lg:py-16 xl:max-w-[1440px]">
        <% if (blogs.length> 0) { %>
        <h1 class="font-bold text-4xl">Recent Post</h1>
        <div class="py-7">
          <div class="grid md:grid-cols-2 gap-6 md:gap-10">
            <% if (blogs.length> 0) { %> <% blogs.slice(0,6).forEach( blog => { %>
            <div
              class="border-2 border-[#007DFE] shadow-lg hover:shadow-xl p-6 rounded-md max-w-xl ease-in-out duration-300"
            >
              <div class="mb-2">
                <a
                  href="/blogs/<%= blog.slug %>"
                  class="text-[#007DFE] font-bold px-1 underline ease-in-out duration-300 cursor-pointer rounded-[5px] text-xl inline-block hover:no-underline hover:bg-blue-300"
                >
                  <%= blog.title %>
                </a>
                <span class="text-gray-500 block">
                  <i class="far fa-calendar text-md mr-1"> </i>
                  <%= blog.createdAt.toLocaleDateString() %>
                </span>
              </div>
              <p class="text-sm md:text-base" id="snippet">
                <%= blog.markdown.split(" ").slice(0, 25).join(" ") %> ....<a
                  href=" /blogs/<%=blog.slug %>"
                  class="text-red-700 cursor-pointer hover:text-red-500"
                  >continue reading</a
                >
              </p>
            </div>
            <% }) %> <% } else { %>
          </div>
          <p>There is no blog to display</p>
          <% } %>
        </div>
        <% } else { %>
        <p>Sorry, we don't have any blog to display right now.</p>
        <% } %>
      </div>
    </section>

    <%-include("./partials/footer.ejs") %>
  </body>
</html>
